import Colors from "../../common/Colors";

const {
  white,
  black
} = Colors;

const style = {
  container: {
    padding: 10,
    backgroundColor: black,
    borderRadius: 4,
    alignItems: 'center',
  },
  textStyle: {
    fontSize: 16,
    color: white,
  },
  gifStyle: {
    width: 40,
    height: 40,
    marginTop: 10,
  }
};

export default style;